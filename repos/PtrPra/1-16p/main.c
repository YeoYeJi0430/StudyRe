#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define BUFFER_SIZE 100
/*
int main(void)
{
	char* words[100];
	int n = 0;
	char buffer[BUFFER_SIZE];
	while (n < 4 && scanf("%s", buffer) != EOF) //scanf("%d", &a) but buffer is 
	{
		words[n] = strdup( buffer ); 
		//words[n] = buffer에서 buffer도 배열의 시작주소를 담고있는 포인터변수
		//buffer가 가지고있는 주소를 words에게 넘김
		//strcpy(str2, str1) str1을 str2로 복사
		//strdup 복제본의 주소를 넘겨줌
		//strdup c++
		n++;
	}
	for (int i = 0; i < 4; i++)
	{
		printf("%s\n", words[i]);
	}
	

	system("pause");
	return EXIT_SUCCESS;
}
*/